# Identity

**Name:** {{NAME}}
**What I am:** One SoloFactory project. The generated application lives at this
repo's root; the frozen contract (PRD, plan, acceptance) lives in `.factory/`;
the Factory Guide transcript and runtime state live in `.aai/memory/` (not committed).

**Purpose:** _(one line — what this project is for; edit freely)_
